name: kolne_app
description: Kolne - 1 min video app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  image_picker: ^1.0.7
  file_picker: ^10.1.9
  video_player: ^2.8.2
  path_provider: ^2.1.2
  permission_handler: ^11.3.1

flutter:
  uses-material-design: true
